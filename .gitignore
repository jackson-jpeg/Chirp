*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
build/
.build/
.DS_Store
Package.resolved
*.ipa
*.dSYM.zip
*.dSYM

# fastlane run artefacts. report.xml records every shell command fastlane ran,
# including ones with credentials interpolated into them. It was committed to a
# PUBLIC repo with the CI keychain password in it. Never track it.
fastlane/report.xml
fastlane/README.md
report.xml

# Signing material and secrets — never track, anywhere in the tree.
*.p8
*.p12
*.mobileprovision
*.cer
.env

# Review contact phone number — repo is public, keep PII out of git.
# The file still syncs to the Mac for deliver runs.
fastlane/metadata/review_information/phone_number.txt
__pycache__/
